(debes crear un venv cada quien y meterle sus requeriments)

comando para crear su entorno:
python -m venv venv
python3 -m venv venv

comando para prender entorno virtual en windows:
venv\Scripts\activate


comando para descargar los requeriments:
pip install -r requirements.txt

hacer los migrate:
python manage.py migrate


--en dado caso la api de elipse este caida pueden ir a esta pagina
https://console.groq.com/

-crean su cuenta le dan a apikeys crear api-key

y pueden crear su propia api-key para luego ponerla en el archivo .env en
GROQ_API_KEY=


-- como prender el script del wiimote
cd wiimote
python3 wiimote_sensor.py --url http://127.0.0.1:8000


-- comando para el config.yaml
set MISTRAL_API_KEY=tu_clave_real_de_mistral_aqui
litellm --config config.yaml --port 4000


Linux:  nmcli device wifi hotspot ifname wlp0s20f3 ssid "RBE-local" password "12345678"


-- para abrir a movil
python manage.py runserver 0.0.0.0:8000
